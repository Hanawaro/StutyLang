var counter = 0
var length = 20
var test = false

for (var iterator = 0; iterators < length / 2; iterator = iterator + 1) {

    print iterator

    if (iterator % 2 == 0 and iterator != 0) {
        print " делится на 2"

        for (;not tests;) {
            iterator = iterator + 1
            test = (iterator % 2 != 0)
        }
        test = false
    }

    println
    counter = counter + iterator * iterator
}

println
println "Сумма всех квадратов выведенных чисел [до " + length / 2 + "] = " + counter


